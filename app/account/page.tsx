import React from 'react'
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Guest area",
};

function page() {
  return (
    <div>Account</div>
  )
}

export default page